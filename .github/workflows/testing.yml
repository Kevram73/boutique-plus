
name: CI for test deployment

on:
  push:
    branches: [ "fix-bugs" ]
  pull_request:
    branches: [ "fix-bugs" ]

  workflow_dispatch:

jobs:
  web-deploy:
    name: 🎉 Deploy
    runs-on: ubuntu-latest
    steps:
    - name: 🚚 Get latest code
      uses: actions/checkout@v2

    - name: 📂 Sync files
      uses: SamKirkland/FTP-Deploy-Action@4.3.2
      with:
        server: 109.234.165.113
        username: boutique@boutique.mingoube.com
        password: Alkashi13!!!%
